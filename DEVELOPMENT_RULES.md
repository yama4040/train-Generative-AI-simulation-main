# 守ってほしいこと（開発ルール・制約）

本ファイルは、本シミュレータ開発で必ず守るべき事項を記録するためのメモです。

## 必須ルール
- **バグのないよう熟考してアウトプットする**
- **既存プログラムのルールを守る**
- **目的達成までのプロセスをTODOとして明記し、実装時は個々のタスクごとに必ずテストする**
- **既存のプログラムにバグがある場合は修正し、機能を実装する**

## 物理・挙動の厳密性
- **列車の挙動は現実の数理モデルに基づく物理演算に従うこと**
- **速度・距離・時間の値を制約達成のために改ざんしない**
- **速度が0なら列車は動かない（瞬間移動不可）**
- **列車は全て同一のルール（数理モデル）で走行する**
- **先行列車と後続列車で処理を変えない**

## 衝突回避・安全
- **列車同士は絶対に接触しない**
- **同一路線・同方向の列車のみ衝突回避対象**
- **安全距離は固定（先行列車の最後尾 + 20m）で変更しない**

## 停車・駅関連
- **駅では必ず停車し、停車時間は設定値に従う**
- **列車は駅の中心に停車する（駅中心と列車中心が一致）**
- **列車長以上の駅は存在しないため、停車不可の駅はない**

## 速度・減速の制約
- **減速度を超えないように駅や停止目標へ停車する**
- **減速開始は停止パターンに基づき、速度0で停止できる位置から開始する**

## 追従運転（IDM）
- **追従時は先行列車の位置・速度を参照する**
- **駅以外での完全停止は極力回避**
- **最低速度20km/hを目標として維持（加速度制限内で段階的に到達）**
- **停止パターン接触時のみ停止許可（最低速度の例外）**
- **5秒未満の頻繁な加減速反復は禁止**
- **既存の衝突回避は必ず維持（常に有効）**

## UI/表示
- **停車時のみ停車時間カウントダウンを表示**
- **走行中は青、停車中は赤など速度に応じた色分け**

## その他
- **シミュレーションは停止ボタンで即時停止できること**
- **円環路線では停止ボタン押下まで継続**
- **非円環路線では全列車が終点到達まで継続**

---

※ このファイルは、今後追加・修正された「守るべき事項」を随時追記する。
